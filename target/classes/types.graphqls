type Query {
    users: [User]
}

type Mutation {
    createUser(firstName: String lastName: String): User
}

type User {
    id: Int!,
    firstName: String,
    lastName: String
}

input UserInput {
    firstName: String,
    lastName: String
}